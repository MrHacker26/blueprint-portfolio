---
slug: refridz
title: Refridz
tagline: Food expiry tracker.
year: "2025"
status: shipped
role: Engineer
stack:
  - TypeScript
  - React Native
  - OCR
  - AI
heroImage: /projects/refridz.png
heroAlt: Refridz feature graphic. Track food expiry dates.
links:
  - label: Play Store
    href: https://play.google.com/store/apps/details?id=com.refridz.app
---

## Overview

Refridz is a small appliance for a boring problem: food that expires in the dark. Capture a label, store a date, get a reminder before the bin does.

## Problem

People do not want a pantry CMS. They will not type every SKU. The capture path has to survive bad lighting, half-cut labels, and a phone that is offline in a basement kitchen.

## Architecture

<Architecture steps="Capture | OCR | Parse | AI fallback | Local store | Reminders" />

OCR is the default. A model is the fallback when the parse is junk. The source of truth stays on device so the app still works when the network does not.

## Failure modes

- OCR miss → try the AI fallback. If that is junk too, ask for a date instead of inventing one.
- No network → inventory and reminders still run from local store. Sync is optional, never required for the core loop.
- Ambiguous parse → do not write a wrong expiry. A missing date is safer than a confident lie.
- Permission or camera failure → the rest of the inventory remains usable.

Offline is not a feature flag. It is the kitchen.

## Tech stack

- React Native client
- On-device storage for inventory
- OCR first (on-device), with a hosted fallback only when confidence is low
- Reminder scheduling that does not depend on a live session

## Challenges

Labels lie. Dates are written in too many formats. OCR confidence is not the same as a correct expiry.

The product has to fail in a way a person can finish the job with a thumb, not a support ticket.

## Tradeoffs

A fully local parse is cheaper and private. It fails on messy labels. A fully hosted parse is smarter and slower, and it fails in the cellar.

The split (OCR, then AI only on miss) keeps the common case fast and the failure case honest.

## Lessons

Failure modes are the product. Design the fallback before the happy path, or the happy path is the only path that works in the demo.

<Callout>
  On Android: [Refridz on Play Store](https://play.google.com/store/apps/details?id=com.refridz.app).
</Callout>

<Gallery />
